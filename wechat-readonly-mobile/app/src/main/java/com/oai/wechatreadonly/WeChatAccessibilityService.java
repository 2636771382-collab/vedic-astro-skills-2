package com.oai.wechatreadonly;

/**
 * Stable manifest entry point.
 * v0.3 implementation lives in WeChatEventSourceService so existing Android
 * accessibility authorization does not need a service-name migration.
 */
public class WeChatAccessibilityService extends WeChatEventSourceService {
}
